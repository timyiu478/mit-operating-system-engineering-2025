# Question

read the paper The Performance of micro-Kernel-Based Systems.

What is one feature/subsystem that's implemented in the kernel in xv6, but is implemented in userspace (i.e., missing from the kernel) in L4? What Unix operations are particularly slow on an L4-based Linux system, and why?

# Answer
